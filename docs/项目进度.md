# VCT 项目进度

> 每次完成新任务后手动更新此文件。记录当前实现、技术方案、待做事项。

---

## 当前状态

Phase 0-1~3 完成，Phase 0-4 编写中，其余待做。

---

## 已实现

### Phase 0-1: 环境搭建（✅ 2026-05-31）

- `requirements.txt` 创建，含 pandas / numpy / scikit-learn / xgboost / streamlit / plotly / statsmodels / joblib
- 依赖已安装（Python 3.13.3 + pip）

### Phase 0-2: 数据下载 + Schema 记录（✅ 2026-05-31）

- 数据已下载到 `data/raw/`，覆盖 VCT 2021-2026
- 数据按年份 `vct_2021~vct_2026` 组织，每年度含 agents/ ids/ matches/ players_stats/
- 全局映射表 `all_ids/` 跨年份可用
- `README.md` 记录了所有 CSV 字段含义
- `.gitignore` 排除 data/ models/ 等目录进版本控制

#### 核心建模表

| 表名 | 路径 | 用途 |
|------|------|------|
| overview | `vct_202X/matches/overview.csv` | 选手级特征（ACS/KAST/ADR/HS%） |
| scores | `vct_202X/matches/scores.csv` | 比赛比分（目标变量） |
| players_stats | `vct_202X/players_stats/players_stats.csv` | 选手赛事级汇总 |
| draft_phase | `vct_202X/matches/draft_phase.csv` | BP 记录（ban/pick） |
| agents_pick_rates | `vct_202X/agents/agents_pick_rates.csv` | 英雄选取率 |

### Phase 0-3: data_pipeline 最小可用版（✅ 2026-05-31）

**实现内容：**
- `src/data_pipeline.py`：读取 raw CSV → 类型转换 → 合并主表 → 输出 parquet

**关键实现决策：**
- scores.csv（match 级）与 overview.csv（player-match-map 级）关联：先将 overview 聚合到 match-team 级别（跨选手取均值），再按 Team A / Team B 分拆合并到 scores
- 百分比字段（KAST、HS%）自动去除 `%` 后缀再转浮点数（原始 CSV 中为 `"78%"` 格式）
- 输出格式：parquet（比 CSV 快 10x，自带列压缩）

**产出：**
- `data/processed/match_dataset.parquet` — 12478 场比赛 × 34 列（含 team_a/team_b 统计量和 target 标签）
- `data/processed/draft_dataset.parquet` — 19164 条 BP 记录

**已知问题：**
- team_a_rating / team_b_rating 空值率约 25%（2023+ 部分 match-team 在 overview 中无对应记录），后续在训练时填充

---

## 技术方案

### 开发策略

先搭地基（数据→特征→模型→预测接口），再做 Match Predictor 完整页面作为**模板页面**，其余两人照着模板复制开发剩余页面。

### 三人分工

```
Phase 0           Phase 1           Phase 2            Phase 3
Person A(地基) →  Person B(模板页) → Person C+A(复制) → 三人(收尾)
```

### 模型方案

- **Match Predictor**: XGBoost + Elo Rating + Logistic Regression 集成
- **BP 预测**: 条件概率 P(ban X | team 选了 Y)
- **BO3 比分**: P(2-0) = P(map1 win) × P(map2 win) 链式乘法
- **特征**: ACS, KAST, ADR, HS%, 历史对战, 近期 form（滚动窗口 5/10/20 场）, 统计差值

### 接口约定

predictor.py 只暴露三个函数给前端：

```python
predict_match(team1, team2)       → {"team1_win_prob": ..., ...}
predict_bp(team1, team2)          → {"ban_prob": ..., "pick_prob": ...}
predict_bo3_score(team1, team2)   → {"2-0": ..., "2-1": ..., "1-2": ..., "0-2": ...}
```

### 代码约定

- **页面三段式**：数据加载 → 计算 → 渲染，`# ====` 分隔
- **先看 `app/components/`** 有无现成组件
- **数据路径**：统一 `DATA_DIR` 常量
- **模型接口**：每个模型实现 `train()` 和 `predict()`
- **类型注解**：src/ 下的函数需 type hints

### 评估指标

| 指标 | 用途 |
|------|------|
| Brier Score / Log Loss | 概率校准质量 |
| ROC-AUC | 区分能力 |
| Accuracy / F1 | 分类准确率 |
| McNemar's Test | 模型间显著性检验 |
| Walk-forward CV | 时序验证（防止数据泄漏） |

---

## 待做事项

### Phase 0-4: 特征工程 + 模型训练
- [ ] 验证 `src/features.py` 跑通
- [ ] `src/features.py`：队伍 form（滚动窗口）、统计差值、历史对战
- [ ] `src/models.py`：训练 XGBoost，Walk-forward CV 验证
- [ ] `src/evaluation.py`：Brier / LogLoss / AUC / F1 / McNemar
- [ ] 保存模型到 `models/`

### Phase 0-5: predictor.py
- [ ] 预测胜负、BP 条件概率、BO3 比分
- [ ] 三个接口函数就绪

### Phase 1-1: Streamlit 主入口
- [ ] app/main.py：导航布局
- [ ] app/components/：公共组件

### Phase 1-2: Match Predictor 模板页
- [ ] app/pages/1_match_predictor.py（带 TEMPLATE 标记）

### Phase 2: 按模板扩展
- [ ] Team Dashboard（app/pages/2_team_analysis.py）
- [ ] Masters London（app/pages/3_masters_london.py）
- [ ] Player Analyzer（app/pages/4_player_analyzer.py）

### Phase 3: 收尾
- [ ] pytest 测试覆盖
- [ ] 集成验证 + README 完善
